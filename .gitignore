# Terraform state files (chứa thông tin nhạy cảm)
*.tfstate
*.tfstate.backup
*.tfstate.lock.info

# Thư mục Terraform cache
.terraform/
.terraform.lock.hcl

# File biến nhạy cảm (chứa IP, key của bạn)
terraform.tfvars
*.auto.tfvars

# OS files
.DS_Store
Thumbs.db
